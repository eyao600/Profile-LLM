[project]
name = "profilellm"
version = "0.1.0"
description = "Standalone automated LLM profiling workflow (JSON + NCU)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "accelerate>=0.26.0",
  "matplotlib>=3.8.0",
  "torch>=2.4.0",
  "transformers>=4.40.0",
]
